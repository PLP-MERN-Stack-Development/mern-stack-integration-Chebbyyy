// tailwind.config.js
import { defineConfig } from 'tailwindcss';

export default defineConfig({
  theme: {
    extend: {},
  },
  plugins: [
    // optional
    require('@tailwindcss/typography'),
  ],
});
